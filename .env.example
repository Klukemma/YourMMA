# Copy to .env and fill in. .env is gitignored - never commit the real key.
# Free key: https://the-odds-api.com/
ODDS_API_KEY=

# Optional path overrides (defaults resolve to engine/data/)
# UFC_CSV=engine/data/UFC_with_mmr_rebuilt_dedup.csv
# PREDICTIONS_LOG=engine/data/prediction_history.json
